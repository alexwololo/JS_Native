// Uppgift: concat
// Instruktioner
// Gör så att a.concat(b) utvärderas till en array med ett innehåll motsvarande [1, 2, 3, 4, 5], och så att a.length === 2 utvärderas till true.

// Ladda upp index.js.

// Ladda upp din lösning

const a = [1, 2, 3]
const b = [4, 5]
const c = a.concat(b)

console.log(c);
