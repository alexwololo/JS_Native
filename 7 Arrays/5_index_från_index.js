// Uppgift: Index från ett index (VG)
// Instruktioner
// Skapa en variabel/kontant a, och tilldela en array till a sådan att…

// alla värden i a är av typen number,
// a[a[0]] + a[1] === 30,
// a[1] === 10, och
// a.length === 3
// … utvärderas till true.

// Informationen från console.log(a, a[a[0]], a[a[0]] + a[1]) kan ge användbar information om uttrycken.

// Ladda upp index.js.

// Ladda upp din lösning


let a = [2, 10, 20];
console.log(a[a[0]])
