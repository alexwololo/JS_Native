// Uppgift: Två parametrar och differens
// Instruktioner
// Skapa en variabel/konstant, difference, och tilldela en funktion till difference.

// Funktionen ska returnera differensen av talen som skickas in som argument till funktionen (det första talet minus det andra).

// difference(1, 1) ska returnera 0 (eftersom 1 - 1 = 0), difference(3, 2) ska returnera 1 (eftersom 3 - 2 = 1), och så vidare.

// Funktionen kan testas med console.log(difference(1, 1)).

// Ladda upp index.js.

// Ladda upp din lösning

let difference = function (x, y=1) {
  return (x-y)
}

console.log(difference(x=1));
