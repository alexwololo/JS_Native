// Uppgift: Object.values och Object.keys
// Instruktioner
// Skapa en konstant/variabel o, och tilldela ett objekt till o sådant att
//Object.values(o)[0] === 'a' && Object.keys(o)[0] === 'b' utvärderas till true.

// De olika Object-metoderna finns dokumenterade på https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object (slå upp metoder under “Methods”).

// Ladda upp index.js.

// Ladda upp din lösning

let o = {
  b: 'a'
};
